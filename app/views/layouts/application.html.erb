<!DOCTYPE html>
<html>
  <head>
    <title>Bugzilla</title>
  
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
      <%= csrf_meta_tags %>
  </head>

  <body>
      
      <!-- <p><%= alert %></p> -->
      <div class="navbar">
        <% if current_user %>
         <!-- <div class= "notice-message"><%= notice %></div> -->
         <%= link_to 'Home', projects_path , :class=>'btn btn-primary' %> 
          <div class = "welcome">
            <strong>Welcome: </strong> <%= current_user.name.upcase %>
            <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
            <br>
          <strong>Logged in as <%=current_user.role.upcase %></strong>
        </div>
         
      </div>

 <% end %>
    <%= yield %>
  </body>
</html>
